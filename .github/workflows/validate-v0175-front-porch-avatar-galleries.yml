name: Validate v0.17.5 Front Porch Avatar Galleries

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: v0175-front-porch-avatar-galleries-${{ github.ref }}
  cancel-in-progress: true

env:
  GODOT_VERSION: '4.7.1'
  GODOT_STATUS: stable

jobs:
  avatar-galleries:
    name: Gallery, Sprite Pack, API and Live UI
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install Godot 4.7.1 and export templates
        run: ./tools/setup_godot_ci.sh

      - name: Import and parse project with warning gate
        shell: bash
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0175-import/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0175-import/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0175-import/config
        run: |
          set -o pipefail
          godot --headless --path . --import 2>&1 | tee /tmp/ccf-v0175-import.log
          if grep -E "SCRIPT ERROR:|Failed to load script|SHADOWED_VARIABLE_BASE_CLASS|SHADOWED_GLOBAL_IDENTIFIER|CONFUSABLE_LOCAL_DECLARATION|INCOMPATIBLE_TERNARY|INTEGER_DIVISION|Parse Error" /tmp/ccf-v0175-import.log; then
            echo "Godot reported a script or warning failure during import."
            exit 1
          fi

      - name: Run focused Front Porch avatar-gallery regression
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0175-focused/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0175-focused/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0175-focused/config
        run: timeout 240s godot --headless --path . --script res://tools/test_v0175_front_porch_avatar_galleries.gd

      - name: Run export-readiness safety regression
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0175-export-safety/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0175-export-safety/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0175-export-safety/config
        run: timeout 240s godot --headless --path . --script res://tools/test_v0175_hotfix2_export_safety.gd

      - name: Recheck Front Porch group cards
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0174/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0174/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0174/config
        run: timeout 240s godot --headless --path . --script res://tools/test_v0174_front_porch_group_cards.gd

      - name: Recheck Front Porch direct character install
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0173/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0173/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0173/config
        run: timeout 180s godot --headless --path . --script res://tools/test_v0173_front_porch_direct_install.gd

      - name: Recheck OpenRouter image hotfix
        env:
          XDG_DATA_HOME: ${{ runner.temp }}/ccf-v0173-hotfix1/data
          XDG_CACHE_HOME: ${{ runner.temp }}/ccf-v0173-hotfix1/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/ccf-v0173-hotfix1/config
        run: timeout 180s godot --headless --path . --script res://tools/test_v0173_hotfix1_openrouter_images.gd

      - name: Confirm Forward Plus remains the desktop default
        shell: bash
        run: |
          grep -F 'config/features=PackedStringArray("4.7", "Forward Plus")' project.godot
          grep -F 'renderer/rendering_method="forward_plus"' project.godot
          grep -F 'rendering_device/fallback_to_opengl3=true' project.godot

      - name: Run quick cross-feature regression profile
        run: python3 ./tools/run_regression_suite.py --manifest ./tools/regression_suites_v0175.json --profile quick --godot "$(command -v godot)"
